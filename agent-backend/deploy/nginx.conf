# ============================================
# nginx.conf - Nginx 反向代理配置（生产环境）
# ============================================
#
# ═══════════════════════════════════════════
# 需要在此文件中编写的 Nginx 配置：
# ═══════════════════════════════════════════
#
# 1. HTTPS 配置（使用 Let's Encrypt 免费证书）：
#    server {
#        listen 443 ssl http2;
#        server_name your-domain.com;
#
#        ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#        ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#        ssl_protocols TLSv1.2 TLSv1.3;
#        ssl_ciphers HIGH:!aNULL:!MD5;
#
#        # 请求体大小限制（防止大文档上传撑爆）
#        client_max_body_size 50m;
#
#        # 代理到后端 uvicorn
#        location / {
#            proxy_pass http://127.0.0.1:8000;
#            proxy_set_header Host $host;
#            proxy_set_header X-Real-IP $remote_addr;
#            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#            proxy_set_header X-Forwarded-Proto $scheme;
#
#            # WebSocket 支持（流式输出需要）
#            proxy_http_version 1.1;
#            proxy_set_header Upgrade $http_upgrade;
#            proxy_set_header Connection "upgrade";
#            proxy_read_timeout 300s;  # 流式响应超时时间
#        }
#
#        # 健康检查路径不过 SSL
#        location /health {
#            access_log off;
#            proxy_pass http://127.0.0.1:8000;
#        }
#    }
#
# 2. HTTP → HTTPS 重定向：
#    server {
#        listen 80;
#        server_name your-domain.com;
#        return 301 https://$server_name$request_uri;
#    }
#
# 3. 限流配置（结合后端的 rate limit）：
#    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=30r/s;
#    location /api/ {
#        limit_req zone=api_limit burst=50 nodelay;
#        proxy_pass http://127.0.0.1:8000;
#    }
#
# ═══════════════════════════════════════════
# 部署步骤：
#   1. 将配置文件放到 /etc/nginx/sites-available/agent-backend
#   2. 软链接到 sites-enabled
#   3. certbot 获取 SSL 证书
#   4. systemctl reload nginx
#
# 非容器部署方式：
#   uvicorn 作为 systemd 服务 + nginx 反向代理
# ═══════════════════════════════════════════
